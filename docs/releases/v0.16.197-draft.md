# v0.16.197 发布草案

**目标版本：** `0.16.197`
**提交范围：** `v0.16.196..HEAD`
**当前阻塞：** 最新本地 release 仍需维护者确认后 push 到远端；本版本把 release draft issue boundary 显式化到整包摘要。

## 用户价值

`v0.16.196` 已经让 `artifact_bundle_summary` 可以展示 candidate issue gate required actions 的首尾边界。维护者和自动化在处理下一版 release draft blocker 时，还需要审阅 `release_draft_issues`，确认第一条和最后一条草案问题是否仍符合预期；目前 summary 已有 primary issue 和完整 issues hash，但只读 summary 的工具仍无法快速确认 issue 列表末尾是否漂移。

`v0.16.197` 在 `artifact_bundle_summary` 中新增 `release_draft_first_issue`、`release_draft_last_issue` 和 `release_draft_issue_boundary_sha256`。这样维护者和自动化可以只读整包摘要，直接确认 release draft issues 的首尾边界、issue count、完整 hash 和 primary issue，再决定是否展开完整 `artifact-manifest.json.release_draft_issues`。

成功标准是：`python scripts/plan_next_iteration.py --target-version 0.16.197 --issues-dir /tmp/cliany-candidate-issues --json` 生成的 `artifact-manifest.json.artifact_bundle_summary` 包含 release draft issue boundary fields，artifacts README 的 `Artifact Bundle Summary` 展示同一字段，且 first/last issue 等于 `artifact-manifest.json.release_draft_issues` 的首尾元素。

## 变更分组

### Release draft issue boundary

- `artifact_bundle_summary` 新增 `release_draft_first_issue`。
- `artifact_bundle_summary` 新增 `release_draft_last_issue`。
- `artifact_bundle_summary` 新增 `release_draft_issue_boundary_sha256`。
- Boundary 从 `artifact-manifest.json.release_draft_issues` 的顺序派生；无 release draft issues 时首尾为 `null`。
- Artifacts `README.md` 的 `Artifact Bundle Summary` 展示 release draft issue first/last item 与 boundary hash。
- 现有 `release_draft_issue_count`、`release_draft_primary_issue`、`release_draft_issues_sha256`、`release_draft_required_action_count`、`release_draft_required_actions_sha256` 和 `release_draft_primary_required_action` 保持兼容。
- 不改变 publication audit、release draft validation 或 candidate issue gate 的判断逻辑。

### 文档与门禁

- [docs/weekly-maintainer-loop.md](../weekly-maintainer-loop.md) 说明 `artifact_bundle_summary` 会展示 release draft issue boundary fields。
- `tests/test_plan_next_iteration.py` 覆盖 manifest summary 与 README summary 的 release draft issue boundary 字段。
- `tests/test_weekly_maintainer_loop_docs.py` 固定维护者循环说明。
- `tests/test_release_draft_docs.py` 固定本草案。

## 案例库映射

本版本不改变案例库状态，继续依赖 [cases/README.md](../../cases/README.md) 和 [cases/manifest.json](../../cases/manifest.json)。`pypi-project-search`、`npm-package-search` 和 `crates-io-crate-search` 仍是 candidate cases；`search-extraction-gap` 继续作为抽取质量 known-gap。

本版本不创建真实 GitHub issue，不创建真实 GitHub Release，不推送 tag，也不把 candidate 标记为 active；它只改善 candidate issue artifacts 的整包摘要可读性和机器可读入口。

## 风险与兼容性

- 新字段只派生自既有 `release_draft_issues` 列表，不新增网络访问或外部状态读取。
- 旧工具忽略未知 summary 字段即可继续读取现有 manifest。
- 新工具可以只读 `release_draft_first_issue`、`release_draft_last_issue`、`release_draft_issue_boundary_sha256`、`release_draft_issue_count`、`release_draft_primary_issue` 和 `release_draft_issues_sha256` 快速确认 release draft issues 是否漂移。
- 不访问 GitHub，不创建远端 tag，不访问第三方业务站点，不使用真实 LLM key。

## 发版前验证

发版前目标版本自检会检查这段锚点：**提交范围：** `v0.16.197..HEAD`。最终带 `--release-tag v0.16.197` 的 strict gate 仍以本草案顶部的 `v0.16.196..HEAD` 作为实际 release diff 范围。

```bash
pytest tests/test_plan_next_iteration.py tests/test_weekly_maintainer_loop_docs.py tests/test_release_draft_docs.py -q --no-cov
ruff check scripts/plan_next_iteration.py tests/test_plan_next_iteration.py tests/test_weekly_maintainer_loop_docs.py tests/test_release_draft_docs.py
python scripts/plan_next_iteration.py --target-version 0.16.197 --json
python scripts/plan_next_iteration.py --target-version 0.16.197 --issues-dir /tmp/cliany-candidate-issues --json
python scripts/release_readiness.py --target-version 0.16.197 --json
```

## 发版步骤

1. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.197 --issues-dir /tmp/cliany-candidate-issues --json` 生成 artifacts。
2. 确认 `artifact-manifest.json.artifact_bundle_summary` 包含 `release_draft_first_issue`、`release_draft_last_issue` 和 `release_draft_issue_boundary_sha256`。
3. 确认 artifacts README 的 `Artifact Bundle Summary` 展示这三个字段。
4. 确认 `release_draft_first_issue` 与 `release_draft_last_issue` 等于 `artifact-manifest.json.release_draft_issues` 的首尾元素。
5. 确认 `release_draft_issue_count`、`release_draft_primary_issue`、`release_draft_issues_sha256` 和完整 `release_draft_issues` 列表仍保持同源。
6. 运行计划器、维护者循环文档和 release draft 相关测试。
7. 将 `CHANGELOG.md` 的 Unreleased 移动到 `0.16.197`。
8. 将 `pyproject.toml` 版本更新为 `0.16.197`。
9. 运行 `python scripts/release_readiness.py --strict --target-version 0.16.197`。
10. 创建 tag：`git tag v0.16.197`。

## Release Notes 摘要

`v0.16.197` adds release draft issue first/last issue and boundary hash fields to the artifact bundle summary, helping tools inspect release draft issue boundaries before expanding the full manifest.
