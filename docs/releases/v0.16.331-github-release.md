# v0.16.331

## Changed

- Candidate issue audit now includes an exact `issue_template_command` and manual-review next action when a candidate issue is genuinely missing.
- The human report shows the same handoff without creating a public issue; the audit remains read-only.

## Compatibility

- Current, stale, duplicate, and unexpected issue-state semantics remain unchanged.
- CLI, adapter commands, SDK, HTTP API, case statuses, and release ordering are unchanged.

## Trust Boundaries

- A missing issue is a maintainer handoff, not permission to create one automatically.
- Candidate package, live LLM, online smoke, and third-party workflow evidence remain pending until real evidence exists.
