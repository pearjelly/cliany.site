# v0.16.329

## Fixed

- Candidate issue audit now protects an attached Doctor Preflight Evidence snapshot from an explicit rewrite that does not provide the current `--doctor-json`.
- The JSON audit reports `doctor_json_required_before_rewrite` and the protected issue numbers; reviewed rewrites with current evidence remain available.

## Compatibility

- CLI, adapter commands, SDK, HTTP API, case statuses, and release ordering are unchanged.

## Trust Boundaries

- Issue-body evidence is a captured doctor preflight snapshot. It does not prove live LLM success, candidate adapter packaging, online smoke, or third-party availability.
