# v0.16.324

## Added

- Added a dated maintainer evidence snapshot for the installed `issues.apache.org` active demo.
- The snapshot records strict static verification and a read-only Apache Spark query with five returned results, plus the commands and trust boundary needed to reproduce it.
- The public quickstart, roadmap, and website now link to the same evidence without presenting a dated third-party result as a current availability guarantee.

## Compatibility

- No CLI, adapter, SDK, HTTP API, case-status, or release-workflow contract changes.

## Trust Boundaries

- The snapshot proves one installed active adapter passed local strict verification and one declared read-only command returned a successful JSON envelope at capture time.
- It does not prove candidate adapter packaging, metadata validation, online smoke, live LLM availability, or continued third-party service availability.
