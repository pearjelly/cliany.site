# v0.16.332

## Fixed

- Candidate issue audit now classifies a GitHub GraphQL `EOF` transport failure as retryable `E_GITHUB_UNAVAILABLE`.
- The audit does not synthesize `missing` state or run an apply path until remote issue state can be read.

## Compatibility

- Issue creation, rewrite confirmation, candidate status semantics, CLI, adapter commands, SDK, HTTP API, and release ordering are unchanged.

## Trust Boundaries

- This release contains no candidate adapter package, live LLM success, online smoke result, or third-party workflow claim.
