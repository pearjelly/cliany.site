# v0.16.357 发布草案

**目标版本：** `0.16.357`

**提交范围：** `v0.16.356..HEAD`（当前未发布修改）
**提交范围：** `v0.16.357..HEAD`（升版后的 target-mode readiness）

状态：未发布。2026-09-22 已发布 354、355、356，门禁要求最早于
2026-09-23 恢复。不得提前创建 tag、发布 PyPI 或部署官网生产版本。

## 用户价值

- 真实保存的适配器可经 CLI、SDK、HTTP、workflow、串行 batch 返回改变输入后的结果；显式 CDP 路由不操作备用浏览器。
- 修复动作被跳过、重复派发、跨命令参数串用、跨页面同名目标误去重，以及提取结果或错误被组合入口丢失的问题。
- 录制语义优于旧编号，冲突名称/角色和最高分并列不再任意命中。模型修复与视觉候选也检查记录语义；这不是通用消歧保证。
- 导航前恢复保存的 Cookie，过期会话阻止导航。内置批量执行器串行运行完整命令，避免共享浏览器与 CLI 输出的线程竞争。
- 探索步数耗尽、损坏回答、缺失命令、错误动作分区不再伪装为完成。
- Jev 提供显式 opt-in 的只读意图定位，不生成操作、不自动点击。需要 TypeSafe 密钥与外发许可；未实测真实模型收益。
- Jev 拒绝超大概率数值和超长候选语义，不再溢出或截断名称后猜测。终端查找显示目标编号、名称、角色与可用分数，JSON 格式不变。
- 保存会话仅保留适用于目标主机的 Cookie，不再保存共享浏览器中其他站点的凭据。
- 完整 CDP 地址保留协议、路径和参数；WebSocket 健康检查与标签页枚举使用 CDP 协议，HTTP 登录页不再被当成有效浏览器。
- Docker 已补齐打包输入并实际构建成功，版本命令与案例目录可用；浏览器启动仍受用户/沙箱及宿主隔离策略限制，不能宣称一键可用。

## 案例库映射

`cases/README.md` 与 `cases/manifest.json`：2 active、3 candidate、2 degraded、1 known-gap。
Jenkins 历史包缺少必需元数据；Confluence 的历史适配器实际使用 title 过滤，
原关键词示例返回零行。不得为通过验收将关键词需求替换为完整标题查询。
`search-extraction-gap` 未关闭，三个候选案例尚无完整晋级证据。

历史 live provider 的 `llm_live_preflight_not_ready` 仍是待重验条件，不把离线模型
替身视作真实模型发现能力。线上 Jira 行结果和 Confluence 审计均为 REST API
行为，不是浏览器回放证据。见 `docs/user-evidence/2026-09-22-confluence-keyword-audit.md`。

## 风险与兼容性

- 旧生成文件不会自动改写。依赖模板、参数归属与去重修复的适配器需要重新探索/生成，先保留旧包，再用变化输入验收。
- 内置 `workflow batch --concurrency N` 现在固定串行并警告；自定义执行器仍可并发。跨进程共享同一浏览器、并行浏览器隔离、明确目标 tab 不在当前保证范围。
- 过去的假成功现在可能返回非零退出或结构化失败；调用方必须检查状态，不应只检查是否有输出。
- 工作流和原子命令要求 stdout 为单个 JSON 对象；诊断放 stderr。显式 `ok` 优先于旧 `success`。
- 名称或角色改变可能要求重新探索。名称包含关系仍允许模糊匹配；仅编号记录仍按编号执行。
- 会话仅验证 Cookie 首次加载恢复，未实现完整 localStorage 恢复或第三方登录验收。
- Cookie 筛选只作用于新保存的会话；旧文件不会自动清理，需重新保存。跨域 SSO 可能需要再次登录，不能为兼容 SSO 恢复全浏览器 Cookie 抓取。
- 完整 ws 地址已通过真实 Chromium 验证；HTTPS/WSS 的地址保留有协议与参数测试，但具体托管服务的 TLS、认证和可达性仍需实测。
- Docker 非 root 加沙箱包在本机复验仍被拒绝创建命名空间；没有使用特权模式或禁用沙箱绕过。见 `docs/user-evidence/2026-09-22-container-smoke.md`。
- Jev 会发送用户意图、元素名称和角色，名称仍可能包含敏感内容。禁止宣称其延迟、成本或准确率已经提升。

## 发版前验证

截至代码提交 `57fce4fc980e06681ab799a7e7395cabf72e12e9`：

- 完整离线测试 3020 项通过；124 个源文件类型检查通过。
- CI `35725549374` 与真实 Chromium CI `35725549329` 均 success。
- 真实 Chromium 验证改变输入、动作计数、歧义零提交、Cookie 首次加载恢复、两行批量结果与备用浏览器不变。
- 新增真实 Chromium 验证完整 WebSocket / HTTP discovery 连接，以及保存的会话不包含另一站点的 Cookie。
- 生成的 Click 命令验证动作顺序、参数归属与跨页面输入保留。
- Confluence 原命令现场仍返回零行，新证据检查正确判失败；该问题没有被修复或晋级。
- 实际探索模型配置存在，但最近一次单次 live preflight 返回 `E_LLM_UNAVAILABLE`，耗时 13397 毫秒，retryable 为 true；不是缺少模型密钥。TypeSafe 密钥仍未配置，未进行真实 Jev 评测。

以上只是已验证代码基线，不等同于未来发布提交。升版后的最终 SHA 必须重跑
完整测试、静态检查、严格案例校验、构建/Twine、精确 SHA 的两条 CI。
还须核对 README、双路线图、官网版本及案例状态，不能只改版本号。

## 独立分发包预验收（2026-09-22）

从干净提交 `87418272201c5366622cfdcbc99efd916125d3d7` 构建 sdist，再从 sdist
构建 wheel，安装至全新 Python 3.11.14 环境；导入位置确认为该环境的
`site-packages`，不是源码目录或 editable 安装。此处是含 Unreleased 修改的
未发布开发构建，版本字段仍为 `0.16.356`，不是 PyPI 上的正式 356 包，禁止上传。

- sdist SHA256：`1441eb0ec58fbc0f857d5318f697d2a8cdd6f5f2eddea47e23553a1d30bc98a3`。
- wheel SHA256：`c90f2f920053721bd68e3b3b45488de9c31cb0683acc9cf0b98713dac8f6cc59`。
- Twine 检查两种分发包均通过；独立解析依赖得到 `browser-use==0.13.10`，不同于源码锁定环境的 `0.12.4`；`uv pip check` 通过。
- 空白临时 HOME 下版本命令正常；未授权外发的 Jev 意图查找返回非零退出及 `E_LLM_DISABLED`。
- 安装声明的 test extra，并按现有 Embodied CI 额外安装测试工具 `playwright==1.60.0` 后，针对已安装 wheel 的 `tests/embodied/test_explore_embodied.py` 全部 16 项通过（74.15 秒）。未安装 Playwright 时模块跳过，不计为通过。
- 验证涵盖真实 Chromium，但模型与 Jev HTTP 响应使用确定性替身；不是完整测试套件在新依赖版本上的验收，也不是实际模型收益或公开站点工作流的证明。

这些哈希仅用于本次开发构建追溯，不包含后续 CDP、Cookie 范围等修复。正式升版后必须重新构建并验证最终分发包。

## 发版步骤

1. 9 月 23 日或之后，先检查是否已有未完成发布；在干净的发布检出拉取最新 master。保留其他任务的 `docs/reviews/`，不删除、不暂存为本版文件。
2. 运行 `python scripts/release_readiness.py --strict --target-version 0.16.357 --remote`，核对日上限和草案；若目标版本已被使用，重新规划，不重复标签。
3. 按现有发布工具升版，移动 Unreleased 到带实际日期的发布条目，同步路线图与官网内容；检查新增依赖 httpx 的锁文件和分发包。
4. 构建并验证包，推送最终 master，等待该精确 SHA 的 CI 与 Embodied CI success。此时才能将候选说明作为已发布说明使用。
5. 创建 `v0.16.357`，运行 `python scripts/release_readiness.py --strict --release-tag v0.16.357 --remote --remote-name origin` 后推送 tag；等待 Release workflow。
6. 核验 GitHub Release 资产与 PyPI 精确版本，再从 `site/` 执行 `vercel link --yes --project cliany.site` 和 `vercel --prod --yes`。
7. 核验生产域名及 home/docs/console，执行 `python scripts/check_release_publication.py --strict --remote --distribution --json`。失败则保持发布未完成，不以标签存在代替三端验收。

下一阶段仍需真实模型探索、三个公开只读任务的独立结果验收，以及外部用户首次上手证据。
