# v0.16.325

## Added

- Added `scripts/capture_active_demo_evidence.py` for bounded, dated active-demo evidence capture.
- The helper derives a strict static verification command and a declared read-only JSON command from `cases/manifest.json`, runs them sequentially, preserves the raw envelopes, and exits nonzero when the gate or read-only command fails.
- The quickstart, maintainer loop, roadmap, and website now point to a real 2026-08-17 `issues.apache.org` / Apache Spark snapshot and state its trust boundary.

## Compatibility

- No CLI, adapter, SDK, HTTP API, case-status, or release-workflow contract changes.

## Trust Boundaries

- The captured result is one dated environment observation. It does not prove that the adapter is a downloadable release asset, candidate package promotion, live LLM availability, or continuing third-party workflow availability.
