# v0.16.337

## Fixed

- Strict release readiness now requires both the public and maintainer roadmaps to state the exact package baseline before a tag can be created.
- macOS `.DS_Store` metadata is ignored so local Finder state does not falsely block a clean release worktree.

## Compatibility

- CLI, adapters, SDK, HTTP API, workflow jobs, and release ordering are unchanged.

## Trust Boundaries

- This release contains no candidate adapter package, live LLM success, online smoke result, or third-party workflow claim.
