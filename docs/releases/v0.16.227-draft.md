# v0.16.227 发布草案

**目标版本：** `0.16.227`
**提交范围：** `v0.16.226..HEAD`
**当前阻塞：** 最新本地 release `v0.16.226` 仍需维护者确认后 push 到远端；本版本先补齐 `v0.16.227` 草案，让下一轮计划不再被 `release draft is missing` 干扰。

## 用户价值

`v0.16.226` 已经把本地 release publication handoff 草案补齐，并完成本地 tag。当前计划里真正排在最前面的待办仍是发布可见性：`publication_visibility.status` 仍是 `local_only`，`candidate_issue_gate.status` 仍是 `blocked_by_publication`，因此不应该创建 candidate issue 或扩大下一版范围。

`v0.16.227` 继续把这个状态写入 release draft：本地已完成 `v0.16.226`，但远端还看不到分支和 tag。维护者读取 `plan_next_iteration.py`、`publication-handoff.json` 或 `release-draft-handoff.json` 时，可以直接判断当前不是缺少草案，而是需要审阅并执行发布同步。

成功标准是：`python scripts/plan_next_iteration.py --target-version 0.16.227 --issues-dir /tmp/cliany-candidate-issues --json` 的 `release_draft_issues` 不再报告 `release draft is missing`；`next_actions` 继续把 `latest local release is not published` 对应的发布同步动作放在首位；当 `tag_points_at_head=false` 时，顶层动作必须明确提示先推 `master`，再选择回到当前 tag commit 或在 HEAD 创建新 release tag，而不是误写成直接推 tag；`publication_next_actions` 和 `publication_publish_commands` 继续明确展示 `git push origin master` 和 `python scripts/check_release_publication.py --remote --json`。

## 变更分组

### Release draft handoff cleanup

- 新增 `docs/releases/v0.16.227-draft.md`，把当前 `v0.16.226` 本地 release 未公开的状态固化到下一版草案。
- 草案明确引用 `publication_visibility`、`publication_next_actions`、`publication_publish_commands`、`candidate_issue_gate`、`publication-handoff.json` 和 `release-draft-handoff.json`。
- 草案说明 `blocked_by_publication` 是当前 candidate issue gate 的预期状态：`v0.16.226` 本地 tag 尚未公开前，不创建真实 GitHub issue。

### Publication action precision

- `plan_next_iteration.py` 现在复用 `check_release_publication.py` 的具体 `publication_next_actions` 作为顶层发布动作。
- 当最新 tag 不指向 HEAD 时，`publication_visibility.summary` 明确写出 `tag_points_at_head=false` 对应的人工选择：回到 tag commit 发布，或在 HEAD 创建新的 release tag。
- 本版本不再把这种状态压缩成 “push branch and tag” 的泛化提示，避免维护者误推过期 tag。
- `check_release_publication.py --publish-script` 生成的脚本在这种状态下写入 `Publication review notes`，明确 `This script will not push that tag automatically`。
- `check_release_publication.py --json` / `--report` 输出 `tag_publish_decision`，其中 `status=manual_decision_required` 和 `can_push_tag=false` 让工具不用解析自然语言也能识别 tag mismatch。
- `plan_next_iteration.py` 把同一判断透传为 `publication_tag_publish_decision`，并写入 Markdown report、`artifact-manifest.json`、`publication-handoff.json` 和 artifacts `README.md` 的 `Publication Handoff`。
- `artifact_bundle_summary` 同步输出 `publication_tag_publish_decision_status`、`publication_tag_can_push`、`publication_tag_required_action_sha256` 和 key/hash 摘要窗口，让只读整包摘要的工具也能识别 tag 发布决策。
- `candidate_issue_gate.evidence` 同步输出 `publication_tag_decision_status`、`publication_tag_can_push` 和 `publication_tag_required_action`，artifacts `README.md` 也展示 `gate_evidence_tag_decision`，让只读取 gate 的工具也能解释 publication block。
- `candidate_issue_gate` 本体同步输出 `primary_reason_code`、`primary_reason_description` 和 `primary_required_action`，artifacts `README.md` 的 `Publication Handoff` 也展示 `gate_primary_reason_code`、`gate_primary_reason_description` 和 `gate_primary_required_action`。
- `publication-handoff.json` 额外输出 `candidate_issue_gate_primary_reason_code`、`candidate_issue_gate_primary_reason_description` 和 `candidate_issue_gate_primary_required_action` 顶层别名，方便只读取 handoff 的工具不用展开 nested gate。
- `artifact_bundle_summary` 同步输出 `publication_handoff_candidate_issue_gate_primary_reason_code`、`publication_handoff_candidate_issue_gate_primary_reason_description` 和 `publication_handoff_candidate_issue_gate_primary_required_action`，让只读整包摘要的工具也能审计 handoff 顶层别名。

### 文档与门禁

- `tests/test_release_draft_docs.py` 固定本草案的必需章节和发布可见性字段。
- `plan_next_iteration.py` 现在输出结构化 `commit_cadence`，并在 Markdown report、`artifact-manifest.json` 和 `artifact_bundle_summary` 中展示 `commit_cadence_status`、`commit_cadence_missing_commit_days`、`commit_cadence_commit_days_sha256` 和 `commit_cadence_next_actions_sha256`，让维护者和自动化能直接跟踪每周提交天数缺口。
- `publication-handoff.json` 和 artifacts `README.md` 的 `Publication Handoff` 也展示 `commit_cadence_status`、`commit_cadence_missing_commit_days` 和 `commit_cadence_primary_next_action`，让维护者先审阅发布同步时也能看到本周提交天数阻塞。
- Candidate issue artifacts `README.md` 现在新增 `Commit Cadence` 小节，集中展示 `commit_day_count`、`min_commit_days`、`missing_commit_days`、`commit_days` 和 `Commit Cadence Next Actions`。
- `artifact_bundle_summary` 现在展示 `commit_cadence_next_action_count` 和 `commit_cadence_primary_next_action`，让只读整包摘要的工具也能直接显示提交节奏下一步。
- 本版本不修改案例库状态，不修改 adapter 生成逻辑，不创建真实 GitHub issue，不推送 `master` 或 tag。
- `release_draft_issues` 清空后，下一轮计划应该只剩发布同步、周提交天数和候选晋级证据这些真实待办。

### Case catalog CLI

- 新增内置 `cliany-site cases` 命令，直接从 CLI 离线列出 active demo、candidate 工作流、离线验证命令和 candidate 晋级下一步。
- `cliany-site cases --case-id <id>` 可精确展开单个案例的命令、validation 和 promotion 详情，找不到案例时会返回当前筛选范围内的可用 case id。
- 单案例的人类输出现在展示目标 URL、文档、样例、validation 和 Promotion Tasks，适合作为贡献者 handoff。
- `cliany-site cases --case-id <id> --issue-template` 可为 candidate 案例输出可复制的 GitHub issue body，并在 `Evidence Bundle` 小节预填 human / JSON 证据包命令。
- `cliany-site cases --case-id <id> --evidence-bundle` 可输出结构化本地证据清单，列出待补 promotion tasks、命令和离线验证步骤。
- `cliany-site cases --case-id <id> --evidence-bundle --json` 现在输出 `status_counts`、`blocked_tasks`、`complete_tasks` 和 `incomplete_tasks`，让维护者不用展开每个 task 也能区分待办、阻塞与已完成项。
- `cliany-site cases --case-id <id> --evidence-bundle --json` 现在也输出 `primary_pending_task`、`primary_blocked_task` 和 `primary_incomplete_task`，方便 issue bot 和 release planner 直接展示首要跟进项。
- `cliany-site cases --case-id <id> --evidence-bundle --json` 现在输出 `primary_next_task`，并在人类 evidence bundle 里展示 `Primary next task`，让维护者和自动化不用重建 pending / blocked / incomplete 优先级规则。
- `cliany-site cases --case-id <id> --evidence-bundle --json`、candidate issue body 和 `issue-metadata.json` 现在输出 `promotion_command_plan`，把 adapter package、metadata validation 和 online smoke 映射到可执行命令。
- `scripts/validate_cases.py --report` 的 `Candidate Promotion Tasks` / `Issue Body Template` 现在也输出 `Promotion Command Plan`，让 case catalog report、cases CLI 和 candidate issue artifacts 使用同一组三步执行命令。
- `scripts/validate_cases.py --json` 的 candidate 条目现在输出 `promotion_command_plan`、`promotion_command_plan_count` 和 `promotion_command_plan_missing_tasks`，让自动化不用解析 Markdown report 就能读取同一组三步晋级命令。
- `scripts/validate_cases.py --json` 和 Markdown report 现在输出顶层 `promotion_command_plan_summary` / `Candidate Promotion Command Plan Summary`，汇总 candidate 命令总数、缺失命令数、缺失 case/task 与 `all_declared` 状态。
- `scripts/plan_next_iteration.py` 现在把 `promotion_command_plan_summary` 透传为 `case_promotion_command_plan_summary`，并写入默认文本输出、Markdown report、candidate issue artifacts manifest 和 `artifact_bundle_summary` compact 字段，包括 `case_promotion_command_plan_summary_sha256`、`case_promotion_command_plan_candidate_count`、`case_promotion_command_plan_command_count`、`case_promotion_command_plan_missing_command_count` 和 `case_promotion_command_plan_all_declared`。
- `scripts/release_readiness.py` 的默认文本输出和 Markdown report 现在也展示 candidate promotion command plan summary，让发版预检 artifact 直接显示 candidate 晋级命令是否完整。
- `scripts/release_readiness.py` 现在把 publication audit 和 `publication_publish_commands` 写入 JSON、默认文本输出和 Markdown report，维护者查看发版预检 artifact 时可直接复制发布可见性复核命令。
- `scripts/release_readiness.py` 现在也把 `publication_tag_publish_decision` 提升到顶层 JSON、默认文本输出和 Markdown report，维护者无需展开 publication audit 就能看到 tag 是否可推和人工决策动作。
- `scripts/release_readiness.py` 现在也把 `publication_next_actions` 和 `publication_next_action_count` 提升到顶层 JSON、默认文本输出和 Markdown report，让发版预检 artifact 直接列出发布可见性待办。
- `scripts/release_readiness.py` 现在也把 `publication_ref_context` 提升到顶层 JSON、默认文本输出和 Markdown report，让维护者直接看到发布待办对应的 branch、upstream、HEAD、tag 和 ahead/behind refs。
- `scripts/release_readiness.py` 现在也把 `publication_worktree_clean`、`publication_worktree_status_count` 和 `publication_worktree_status` 提升到顶层 JSON、默认文本输出和 Markdown report，让维护者在发版预检 artifact 里直接判断本地改动是否阻塞发布命令。
- `scripts/release_readiness.py` 现在也输出 `publication_primary_next_action` 和 `publication_primary_publish_command`，让只读发版预检摘要的工具可以直接展示首要发布待办与首条发布命令。
- `scripts/release_readiness.py` 现在也输出 `publication_next_actions_sha256` 和 `publication_publish_commands_sha256`，让发版预检 artifact 能直接检测 publication action/command 列表是否漂移。
- `scripts/release_readiness.py` 现在输出顶层 `publication_summary`，把发布状态、worktree、branch/tag、ahead/behind、tag 决策、首要动作和首条发布命令压缩成一个机器可读摘要。
- `scripts/release_readiness.py` 现在输出 `publication_summary_sha256`，让只读发版预检 artifact 的工具能检测 publication summary 是否漂移。
- `scripts/release_readiness.py` 现在也输出 `publication_summary_primary_next_action` 和 `publication_summary_primary_publish_command` 顶层别名，让只读摘要字段族的工具不用展开 nested summary。
- `scripts/release_readiness.py` 的 `publication_tag_publish_decision` 和 `publication_summary` 现在也附带 `target_tag`、`target_tag_status`、`target_tag_primary_command` 和 `target_tag_commands_sha256`，让发版预检入口与周计划入口共享目标 tag 交接字段。
- `scripts/release_readiness.py --json` 的顶层 `next_actions` 现在会在 publication 未公开时优先展示 `publication_summary.primary_next_action`，再展示 cadence/readiness 修复动作。
- `scripts/release_readiness.py --json` 现在输出 `publication_blockers`、`publication_primary_blocker` 和 `publication_blockers_sha256`，让发版预检 artifact 顶层直接展示发布可见性阻塞原因。
- `scripts/release_readiness.py --json` 顶层现在也展示 `next_action_count`、`primary_next_action` 和 `next_actions_sha256`，让维护工具不用展开 `next_actions` 就能显示首要发版预检动作并检测漂移。
- `scripts/release_readiness.py --json` 的顶层 `next_actions` 现在保持纯文本，不再混入 Markdown bullet；默认文本输出和 Markdown report 仍由渲染层添加列表符号。
- `scripts/check_release_publication.py` 的 JSON `next_actions` 现在保持纯文本，不再混入 Markdown bullet；文本和 Markdown report 仍由渲染层添加列表符号。
- `scripts/check_release_publication.py` 现在也输出 `primary_next_action`、`next_actions_sha256`、`primary_publish_command` 和 `publish_commands_sha256`，让 publication audit 自身也能只读首项与 hash。
- `scripts/check_release_publication.py --publish-script` 生成的可审阅发布脚本现在在注释头中展示 next/publish 的 count、SHA-256 和 primary 字段，维护者执行前即可确认脚本对应的发布动作集。
- `scripts/check_release_publication.py --publish-script` 生成的可审阅发布脚本现在执行前会重新运行 publication audit，并在 next/publish hash 已漂移时退出，避免继续执行过期发布命令。
- `scripts/check_release_cadence.py` 的 JSON `next_actions` 现在保持纯文本，并输出 `primary_next_action` 与 `next_actions_sha256`，让 cadence audit 与 publication audit 的 action contract 保持一致。
- `scripts/plan_next_iteration.py` 现在也在顶层 JSON、默认文本输出和 Markdown report 展示 `next_action_count`、`primary_next_action` 与 `next_actions_sha256`，让周计划自身和 cadence/publication action contract 对齐。
- `scripts/plan_next_iteration.py --issues-dir` 的 `artifact-manifest.json` 顶层现在也展示 `next_action_count`、`primary_next_action` 与 `next_actions_sha256`，让只读 manifest 的维护工具无需重算周计划 action 摘要。
- `scripts/plan_next_iteration.py` 现在把 `publication_next_actions_sha256`、`publication_primary_next_action`、`publication_publish_commands_sha256` 和 `publication_primary_publish_command` 提升到顶层 JSON、默认文本输出和 Markdown report。
- `scripts/plan_next_iteration.py` 现在也透传 `publication_blockers`、`publication_primary_blocker` 和 `publication_blockers_sha256` 到顶层 JSON、默认文本输出、Markdown report 与 candidate issue artifacts，帮助维护者先看发布阻塞原因再执行同步命令。
- `scripts/plan_next_iteration.py` 的 `publication_tag_publish_decision` 现在附带 `target_tag`、`target_tag_status`、`target_tag_commands` 和 `target_tag_commands_sha256`，当 latest tag 不指向 HEAD 时会明确给出创建目标 release tag 的命令。
- `scripts/plan_next_iteration.py` 的顶层 `next_actions` 现在会在 tag mismatch 时展示目标 release tag 动作和命令，不再把旧 tag 的 push 提示放进首要维护者动作列表。
- README / README.zh 的路线图入口现在也说明维护者可读取 `primary_next_action`，自动化可比对 `next_actions_sha256`、`publication_next_actions_sha256` 和 `publication_publish_commands_sha256` 检测发布动作漂移。
- `docs/weekly-maintainer-loop.md` 现在说明维护者应优先读取 `publication_summary.status`、`publication_summary.worktree_clean`、`publication_summary.primary_next_action` 和 `publication_summary.primary_publish_command`，再按需展开发布详情。
- `scripts/validate_cases.py --report` 现在输出 `Candidate Evidence Bundle Commands` 小节，直接链接每个 candidate 的 evidence bundle CLI 命令。
- `scripts/validate_cases.py --report` 的 `Issue Body Template` 也带 `Reproduction Context` 和 `Evidence Bundle` 小节，避免维护者从 case report 复制旧版 issue body。
- `scripts/plan_next_iteration.py --issues-dir` 生成的 `candidate_promotions`、`issue-metadata.json`、issue metadata summary 和 artifacts `README.md` 现在输出 `evidence_bundle_command` / `evidence_bundle_json_command`，`Candidate Summary` 可直接复制 evidence bundle 命令。
- `scripts/plan_next_iteration.py` 的 candidate promotion JSON、`issue-metadata.json` 和 artifacts `README.md` 现在输出 `promotion_evidence_primary_task`，`Candidate Summary` 可直接展示首要 evidence task。
- `scripts/plan_next_iteration.py` 的 candidate promotion JSON、`issue-metadata.json` 和 artifacts `README.md` 现在也输出 `evidence_bundle_primary_next_task`，并在 `Candidate Summary` 表展示 `Evidence Bundle Primary Next Task`，让 `cliany-site cases --case-id <id> --evidence-bundle --json` 的首要下一步字段贯穿周计划与 issue artifacts。
- `scripts/plan_next_iteration.py` 的默认文本输出和 Markdown report 现在也展示 `evidence_bundle_primary_next_task`，让维护者不用生成 issue artifacts 也能看到 evidence bundle 首要下一步。
- `docs/weekly-maintainer-loop.md` 现在同步说明 `promotion_evidence_primary_task` 与 `Primary Evidence Task`，让周维护手册能准确描述 candidate issue artifacts 的首要 evidence task 字段。
- `scripts/plan_next_iteration.py` 生成的 candidate issue body 也带 `Evidence Bundle` 小节，让 artifacts body 文件和 CLI `--issue-template` 输出保持同一套交接字段。
- `cliany-site cases --case-id <id> --issue-template` 和 `scripts/plan_next_iteration.py` 生成的 candidate issue body 现在会把已有 evidence 的 `complete` promotion task 输出为 `- [x]`，让 issue 正文保留已完成证据状态。
- `cliany-site cases --case-id <id> --issue-template` 和 `scripts/plan_next_iteration.py` 生成的 candidate issue body 现在包含 `Primary Evidence Task` 小节，直接展示当前首要待补 evidence task、状态、证据和下一步。
- `cliany-site cases --case-id <id> --issue-template --json` 现在输出 `issue_template_primary_task`，让 issue bot 不用解析 Markdown 就能读取首要待补证据任务。
- `cliany-site cases --json` 的 `promotion_evidence_summary` 现在输出 `primary_task_detail`，直接暴露首要 candidate 晋级任务的 case、task、status、evidence 和 next action。
- `scripts/validate_cases.py` 的 `promotion_evidence_summary` 也输出 `primary_task_detail`，Markdown report 的 `Candidate Promotion Evidence Summary` 展示同源字段，让 case catalog CLI 和校验报告保持一致。
- `scripts/plan_next_iteration.py` 在回退构造 `case_promotion_evidence_summary` 时也保留 `primary_task_detail`，避免周计划因输入来源不同而丢失首要任务结构化别名。
- `cliany-site cases --json`、`scripts/validate_cases.py` 和 `scripts/plan_next_iteration.py` 的 promotion evidence summary 现在输出 `primary_next_task`，让全局 candidate summary 与单案例 evidence bundle 使用同一首要下一步字段名。
- `scripts/plan_next_iteration.py` 的顶层 JSON、默认文本输出和 Markdown report 现在直接展示 `case_promotion_evidence_primary_next_task` 与 `case_promotion_evidence_primary_next_action`，让维护工具不用展开 summary 也能定位首要 candidate 任务。
- `scripts/plan_next_iteration.py` 的顶层 JSON、默认文本输出和 Markdown report 现在也展示 `case_promotion_evidence_summary_sha256` 与 `case_promotion_command_plan_summary_sha256`，让周计划消费者不用生成 artifacts 也能检测 candidate evidence / command plan 摘要漂移。
- `scripts/plan_next_iteration.py` 现在输出 `plan_report_command`，把生成 `/tmp/cliany-next-iteration.md` 的 Markdown 周计划命令与 `issue_artifacts_command` 一样结构化交给维护工具。
- Candidate issue artifacts 的 `validation_commands` 现在包含 `plan_report_command`，让 artifacts README 和 manifest 都能直接复现 Markdown 周计划报告。
- Candidate issue artifacts 的 `artifact-manifest.json` 现在带顶层 `plan_report_command`，`artifact_bundle_summary` 也输出 `plan_report_command_sha256`，让维护工具不用展开 validation commands 就能复现并检测周计划报告命令漂移。
- Candidate issue artifacts 的 `publication-handoff.json` 和 README `Publication Handoff` 现在也展示 `plan_report_command`，让处理发布门禁的维护工具能直接跳回同一份 Markdown 周计划报告。
- Candidate issue artifacts 的 `release-draft-handoff.json` 和 README `Release Draft Handoff` 现在也展示 `plan_report_command`，让处理草案门禁的维护工具能直接复现同一份 Markdown 周计划报告。
- Candidate issue artifacts 的 `publication-handoff.json`、`release-draft-handoff.json` 和对应 README handoff 小节现在也展示 `issue_artifacts_command`，让只读取 handoff 的维护工具能直接重新生成整包候选任务 artifacts。
- Candidate issue artifacts 的 `publication-handoff.json`、`release-draft-handoff.json` 和对应 README handoff 小节现在也展示 `plan_report_command_sha256` 与 `issue_artifacts_command_sha256`，让只读取 handoff 的维护工具能检测复现命令漂移。
- `artifact_bundle_summary` 现在输出 `case_promotion_evidence_primary_detail_sha256`，让只读整包摘要的维护工具能检测首要 candidate 晋级任务对象漂移。
- `artifact_bundle_summary` 现在也输出 `case_promotion_evidence_primary_next_task_sha256`，让只读整包摘要的维护工具能单独检测 `primary_next_task` 漂移。
- `docs/weekly-maintainer-loop.md` 现在同步说明 `case_promotion_evidence_primary_next_task_sha256`，让周维护手册覆盖最新 artifact bundle summary 字段。
- `cliany-site cases --status candidate` 的人类输出现在会在 `Candidate 下一步` 中展示首要任务状态和当前 evidence，并从 `primary_next_task` 读取首要任务，让维护者不用切到 JSON 也能判断证据是否已附加。
- `scripts/validate_cases.py` 的默认文本输出现在展示 `promotion_evidence_primary_next_task`，让快速校验入口也使用同一首要下一步字段名。
- `scripts/validate_cases.py` 的默认文本输出现在展示 `promotion_evidence_primary` 和 `promotion_evidence_evidence`，让维护者不用打开 Markdown report 也能看到首要 candidate 晋级任务状态与证据。
- `scripts/release_readiness.py --report` 现在输出 `Candidate Primary Next Task` 小节，直接展示 `promotion_evidence_summary.primary_next_task` 的 case、task、status、evidence 和 next action。
- `scripts/release_readiness.py --report --packages-dir ...` 现在输出 `Case Package Checks` 小节，直接列出每个 case adapter 包的状态、路径、问题和修复动作。
- `scripts/release_readiness.py --json --packages-dir ...` 的 `next_actions` 现在会透传失败 case package 的修复动作，让自动化不用解析 Markdown 也能提示缺失或无效包的下一步。
- `scripts/release_readiness.py --json` 的 `package_gate` 现在输出 `failed_count`、`missing_count`、`invalid_count`、`repair_action_count` 和 `primary_repair_action`，让维护工具不用遍历 case 列表也能判断包资产修复规模。
- `scripts/release_readiness.py` 的默认文本输出现在展示 `package_gate_summary` 和 `package_gate_primary_repair_action`，维护者不生成 JSON/Markdown 也能看到包资产修复规模和首要动作。
- `scripts/release_readiness.py --require-packages` 现在会在已检查包但存在失败项时让 `package_gate.ok=false`，并输出 `case package validation failed` blocker，避免正式发版预检把无效包只归到通用 case catalog 失败。
- `scripts/release_readiness.py` 的 package repair `next_actions` 现在会把多个 case 共用的修复动作合并为一条 `Package checks` 动作，避免空 packages 目录下重复输出同一条 rerun 命令。
- `scripts/plan_next_iteration.py` 现在支持 `--packages-dir` 和 `--require-packages`，会把包门禁参数透传给 release readiness，并写入 validation commands 与 issue artifacts 复现命令。
- `scripts/validate_cases.py --include-candidate-packages` 现在可显式校验 candidate adapter 包，按 `adapter_domain-<version>.cliany-adapter.tar.gz` 约定定位包文件，方便给 `pypi-project-search` 等候选案例补齐 adapter package evidence。
- `scripts/plan_next_iteration.py --packages-dir ... --json` 的 validation commands 现在会包含 `python scripts/validate_cases.py --packages-dir ... --include-candidate-packages --strict`，让周初计划直接给出 candidate 包 evidence 的复现命令。
- Candidate 案例的 `metadata_validation` 和 `promotion_evidence.metadata_validation.next_action` 现在统一使用 `--include-candidate-packages`，避免维护者照着 evidence 清单运行默认 active 包校验而漏掉候选包。
- `cliany-site cases --case-id <id> --evidence-bundle --json` 现在输出 `candidate_package_validation_command`，人类 evidence bundle 也展示 `Candidate package validation` 小节，让候选包 evidence 命令无需从自然语言里解析。
- `scripts/plan_next_iteration.py` 的 `candidate_promotions`、`issue-metadata.json` 和 artifacts `README.md` 现在也输出 `candidate_package_validation_command`，让候选包 evidence 命令贯穿周计划和 issue artifacts。
- Candidate issue body 和 `cliany-site cases --case-id <id> --issue-template` 现在在 `Validation Evidence` 中直接展示 candidate package validation command，减少创建 issue 后的命令查找。
- Candidate issue artifacts 的 review checklist 现在要求核对 `issue-metadata.json` 中的 `candidate_package_validation_command`，让候选包验证命令进入创建 issue 前的人工门禁。
- README / README.zh 现在用 `promotion_evidence_summary.primary_next_task` 指引自动化读取首要 candidate 任务，避免新用户继续参考旧的 `primary_task_detail` 字段。
- `cliany-site cases --status candidate --detail --json` 会输出 candidate promotion evidence summary，方便维护者不用打开 `cases/manifest.json` 也能看到首要晋级动作。
- `cases/README.md` 现在说明 `Candidate Promotion Evidence Summary` 会展示 `primary_next_task`，帮助贡献者直接定位首要 candidate case/task/status/evidence。
- `docs/good-first-issues.md` 和 `docs/contributor-starter.md` 现在要求 candidate promotion issue 引用 evidence bundle 的 `primary_next_task`，让 good-first issue 指向首要 case/task/status/evidence。
- `docs/good-first-issues.md` 现在要求 candidate promotion issue 附上 `cliany-site cases --case-id <id> --evidence-bundle --json` 输出，避免 good-first issue 缺少机器可读证据包，并让 issue 描述与 `promotion_evidence` 保持一致。
- `docs/good-first-issues.md` 和 `docs/contributor-starter.md` 现在同步说明 candidate promotion 的 Issue Body Template 会保留 `Reproduction Context` 和 `Evidence Bundle`，让首次贡献者知道 issue 正文应包含复现上下文和机器可读证据包。
- wheel 构建现在把 `cases/` 索引和样例输出纳入 `cliany_site/cases/` 资源，确保从 PyPI 安装后仍能查看案例库。

## 案例库映射

本版本不改变案例库状态，继续依赖 [cases/README.md](../../cases/README.md) 和 [cases/manifest.json](../../cases/manifest.json)。`pypi-project-search`、`npm-package-search` 和 `crates-io-crate-search` 仍是 candidate cases；`search-extraction-gap` 继续作为抽取质量 known-gap。

Candidate promotion 的首要任务仍是 `pypi-project-search` 的 `adapter_package` evidence。发布可见性完成前，本版本不把 candidate issue artifacts 变成真实 GitHub issue，也不把任何 candidate 标记为 active。

## 风险与兼容性

- 这是发布流程计划输出、文档、测试锚点和内置只读案例浏览命令更新，不改变 CLI adapter 运行时和案例库状态。
- `publication_visibility` 仍以 `check_release_publication.py` 的真实 Git 状态为准。
- `candidate_issue_gate` 仍应在 `v0.16.226` 未发布时保持 `blocked_by_publication`。
- 不访问 GitHub，不创建远端 tag，不访问第三方业务站点，不使用真实 LLM key。

## 发版前验证

完成 `v0.16.227` 后，下一轮目标版本自检会检查这段锚点：**提交范围：** `v0.16.227..HEAD`。当前带 `--release-tag v0.16.227` 的 strict gate 仍以本草案顶部的 `v0.16.226..HEAD` 作为实际 release diff 范围。

```bash
pytest tests/test_release_draft_docs.py -q --no-cov
ruff check tests/test_release_draft_docs.py
python scripts/plan_next_iteration.py --target-version 0.16.227 --issues-dir /tmp/cliany-candidate-issues --json
python scripts/check_release_publication.py --json
python scripts/check_release_publication.py --remote --json
python scripts/release_readiness.py --target-version 0.16.227 --json
python scripts/release_readiness.py --strict --target-version 0.16.227 --report /tmp/cliany-release-readiness.md
cliany-site cases --status candidate --detail --json
cliany-site cases --case-id pypi-project-search --json
cliany-site cases --case-id pypi-project-search --issue-template
cliany-site cases --case-id pypi-project-search --evidence-bundle
cliany-site cases --case-id pypi-project-search --evidence-bundle --json
```

## 发版步骤

1. 确认 `tests/test_release_draft_docs.py` 和 ruff 通过。
2. 确认 `plan_next_iteration.py --target-version 0.16.227 --issues-dir` 不再报告 `release draft is missing`，且在 `tag_points_at_head=false` 时不会提示直接推旧 tag。
3. 确认 `publication_tag_publish_decision.status`、`artifact_bundle_summary.publication_tag_publish_decision_status`、`candidate_issue_gate.evidence.publication_tag_decision_status` 与 `publication-handoff.json` 中的 `tag_publish_decision.status` 一致，且 `candidate_issue_gate.status` 在 `v0.16.226` 未发布前仍是 `blocked_by_publication`。
4. 确认 `candidate_issue_gate.primary_reason_code`、`candidate_issue_gate.primary_reason_description`、`candidate_issue_gate.primary_required_action` 与 `publication-handoff.json` 的 `candidate_issue_gate_primary_*` 顶层别名、`artifact_bundle_summary.publication_handoff_candidate_issue_gate_primary_*`、artifacts `README.md` 的 `gate_primary_*` 展示一致。
5. 确认 `commit_cadence.status`、`commit_cadence.missing_commit_days`、`commit_cadence.next_actions[0]` 与 `publication-handoff.json` 的 `commit_cadence_*`、`artifact_bundle_summary.commit_cadence_*` 和 artifacts `README.md` 的 `Commit Cadence` 小节一致，且当前缺口仍要求本周继续提交到至少 3 个独立日期。
6. 确认 `cliany-site cases --case-id pypi-project-search --issue-template` 输出 `Evidence Bundle` 小节，并预填 `cliany-site cases --case-id pypi-project-search --evidence-bundle --json`。
7. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的 `candidate_promotions[0].issue_body` 也包含同一组 `Evidence Bundle` 命令。
8. 确认 candidate issue body 对已有 evidence 的 `complete` promotion task 输出 `- [x]`，pending/blocked task 仍输出 `- [ ]`。
9. 确认 candidate issue body 包含 `## Primary Evidence Task`，并且已完成任务不会被选为首要待补证据任务。
10. 确认 `cliany-site cases --case-id pypi-project-search --issue-template --json` 输出 `issue_template_primary_task`，且与 `Primary Evidence Task` 小节指向同一 task。
11. 确认 `cliany-site cases --status candidate --json` 的 `promotion_evidence_summary.primary_task_detail` 与 legacy `primary_case_id`、`primary_task` 和 `primary_next_action` 同源。
12. 确认 `promotion_evidence_summary.primary_next_task` 与 `primary_task_detail` 同源。
13. 确认 `python scripts/validate_cases.py --report /tmp/cliany-case-catalog-report.md` 的 `Candidate Promotion Evidence Summary` 展示 `primary_task_detail`，且 JSON report 中该字段与 `primary_task` 同源。
14. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的 `case_promotion_evidence_summary.primary_task_detail` 与 `primary_task` 同源。
15. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的 `case_promotion_evidence_summary.primary_next_task` 与 `primary_task_detail` 同源。
16. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的顶层 `case_promotion_evidence_primary_next_task` 与 `case_promotion_evidence_summary.primary_next_task` 同源，顶层 `case_promotion_evidence_primary_next_action` 与 summary 的 `primary_next_action` 同源，Markdown report 指标表也展示这两个字段。
17. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的顶层 `case_promotion_evidence_summary_sha256` 与 `case_promotion_command_plan_summary_sha256` 和对应 summary 对象同源，默认文本输出与 Markdown report 也展示同名指标。
18. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 输出 `plan_report_command`，且命令为 `python scripts/plan_next_iteration.py --target-version 0.16.227 --report /tmp/cliany-next-iteration.md`。
19. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --issues-dir /tmp/cliany-candidate-issues --json` 生成的 `artifact-manifest.json.validation_commands` 和 artifacts `README.md` 都包含 `plan_report_command`。
20. 确认 `artifact-manifest.json.plan_report_command` 与顶层 `plan_report_command` 同源，且 artifacts `README.md` 的 `Artifact Bundle Summary` 展示 `plan_report_command_sha256`。
21. 确认 `publication-handoff.json.plan_report_command` 与顶层 `plan_report_command` 同源，且 artifacts `README.md` 的 `Publication Handoff` 展示同一命令。
22. 确认 `release-draft-handoff.json.plan_report_command` 与顶层 `plan_report_command` 同源，且 artifacts `README.md` 的 `Release Draft Handoff` 展示同一命令。
23. 确认 `publication-handoff.json.issue_artifacts_command` 和 `release-draft-handoff.json.issue_artifacts_command` 与顶层 `issue_artifacts_command` 同源，且 artifacts `README.md` 的两个 handoff 小节都展示同一命令。
24. 确认 `publication-handoff.json.plan_report_command_sha256`、`publication-handoff.json.issue_artifacts_command_sha256`、`release-draft-handoff.json.plan_report_command_sha256` 和 `release-draft-handoff.json.issue_artifacts_command_sha256` 与对应命令同源，且 artifacts `README.md` 的两个 handoff 小节展示同一组 hash。
25. 确认 `artifact_bundle_summary.case_promotion_evidence_primary_detail_sha256` 与 `case_promotion_evidence_summary.primary_task_detail` 同源。
26. 确认 `artifact_bundle_summary.case_promotion_evidence_primary_next_task_sha256` 与 `case_promotion_evidence_summary.primary_next_task` 同源。
27. 确认 `cliany-site cases --status candidate` 的 `Candidate 下一步` 从 `primary_next_task` 显示首要任务状态和当前 evidence。
28. 确认 `python scripts/validate_cases.py` 默认文本输出展示 `promotion_evidence_primary_next_task`、`promotion_evidence_primary` 和 `promotion_evidence_evidence`。
29. 确认 `python scripts/release_readiness.py --report /tmp/cliany-release-readiness.md` 展示 `Candidate Primary Next Task`，并与 `cases.promotion_evidence_summary.primary_next_task` 同源。
30. 确认 `python scripts/release_readiness.py --packages-dir ~/.cliany-site/packages --report /tmp/cliany-release-readiness.md` 展示 `Case Package Checks`，并列出 package status、path、issues 和 next actions。
31. 确认 `python scripts/release_readiness.py --json --packages-dir ~/.cliany-site/packages` 的 `next_actions` 透传失败 case package 的修复动作。
32. 确认 `python scripts/release_readiness.py --json --packages-dir ~/.cliany-site/packages` 的 `package_gate` 展示 `failed_count`、`missing_count`、`invalid_count`、`repair_action_count` 和 `primary_repair_action`。
33. 确认 `python scripts/release_readiness.py --packages-dir ~/.cliany-site/packages` 的默认文本输出展示 `package_gate_summary` 和 `package_gate_primary_repair_action`。
34. 确认 `python scripts/release_readiness.py --require-packages --packages-dir ~/.cliany-site/packages --json` 会在已检查包但有失败项时输出 `package_gate.ok=false` 和 `case package validation failed` blocker。
35. 确认 `python scripts/release_readiness.py --json --packages-dir ~/.cliany-site/packages` 的 package repair `next_actions` 会把多个 case 共用的 rerun 动作合并为一条 `Package checks`。
36. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --packages-dir ~/.cliany-site/packages --require-packages --json` 会把包门禁参数写入 validation commands 与 issue artifacts 复现命令。
37. 确认 `python scripts/validate_cases.py --packages-dir ~/.cliany-site/packages --include-candidate-packages --strict` 会校验 candidate adapter 包，并在缺失 `pypi.org-<version>.cliany-adapter.tar.gz` 时输出 candidate package repair action。
38. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --packages-dir ~/.cliany-site/packages --json` 的 validation commands 包含 `python scripts/validate_cases.py --packages-dir ~/.cliany-site/packages --include-candidate-packages --strict`。
39. 确认 `cases/manifest.json` 中每个 candidate 的 `metadata_validation` 和 `promotion_evidence.metadata_validation.next_action` 都包含 `--include-candidate-packages`。
40. 确认 `cliany-site cases --case-id pypi-project-search --evidence-bundle --json` 输出 `candidate_package_validation_command`，且人类 evidence bundle 展示 `Candidate package validation`。
41. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --issues-dir /tmp/cliany-candidate-issues --json` 输出的 `candidate_promotions`、`issue-metadata.json` 和 artifacts `README.md` 都展示 `candidate_package_validation_command`。
42. 确认 `cliany-site cases --case-id pypi-project-search --issue-template` 和 candidate issue body 的 `Validation Evidence` 都展示 candidate package validation command。
43. 确认 candidate issue artifacts 的 review checklist 要求核对 `issue-metadata.json` 中的 `candidate_package_validation_command`。
44. 确认 `python scripts/validate_cases.py --report /tmp/cliany-case-catalog-report.md` 的 `Issue Body Template` 也包含 `Reproduction Context`、`Promotion Command Plan` 和 `Evidence Bundle`。
45. 确认 candidate issue artifacts 的 `issue-metadata.json` 和 `Candidate Summary` 同时展示 `evidence_bundle_command`、`evidence_bundle_json_command` 与 `promotion_evidence_primary_task`，且命令能输出 `pypi-project-search` 的证据包。
46. 确认 candidate issue artifacts 的 `issue-metadata.json` 和 `Candidate Summary` 同时展示 `evidence_bundle_primary_next_task`，且与 evidence bundle JSON 的 `primary_next_task` 同源。
47. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227` 默认文本输出和 Markdown report 都展示 `evidence_bundle_primary_next_task`。
48. 确认 `cliany-site cases --case-id pypi-project-search --evidence-bundle --json` 输出 `status_counts`、`blocked_tasks`、`complete_tasks`、`incomplete_tasks`、`primary_pending_task`、`primary_blocked_task` 和 `primary_incomplete_task`，且 `incomplete_task_count` 与未完成任务列表一致。
49. 确认 `cliany-site cases --case-id pypi-project-search --evidence-bundle --json` 输出 `primary_next_task`，且人类 evidence bundle 展示 `Primary next task`。
50. 确认 `cliany-site cases --case-id pypi-project-search --evidence-bundle --json`、candidate issue body 和 `issue-metadata.json` 都输出 `promotion_command_plan`，且包含 adapter package、metadata validation 和 online smoke 三步命令。
51. 确认 `python scripts/validate_cases.py --json` 的 candidate case 条目输出 `promotion_command_plan`、`promotion_command_plan_count=3` 和空的 `promotion_command_plan_missing_tasks`。
52. 确认 `python scripts/validate_cases.py --json` 顶层 `promotion_command_plan_summary.all_declared=true`、`missing_command_count=0`，且 Markdown report 展示 `Candidate Promotion Command Plan Summary`。
53. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 顶层输出 `case_promotion_command_plan_summary`，且 `artifact-manifest.json` / `artifact_bundle_summary` 展示 command plan summary 的 hash、candidate count、command count、missing count 和 `all_declared`。
54. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --report /tmp/cliany-release-readiness.md` 展示 `Candidate Promotion Command Plan Summary`，默认文本输出展示 `candidate_command_plan_summary`。
55. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_publish_commands`，Markdown report 展示 `Publication Publish Commands`。
56. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_tag_publish_decision.status=manual_decision_required` 与 `can_push_tag=false`，Markdown report 展示 `tag_publish_decision`。
57. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_next_actions` 与 `publication_next_action_count`，Markdown report 展示 `Publication Next Actions`。
58. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_ref_context`，Markdown report 展示 `Publication Ref Context`。
59. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_worktree_clean`、`publication_worktree_status_count` 和 `publication_worktree_status`，Markdown report 展示 `Publication Worktree Status`。
60. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_primary_next_action`、`publication_next_actions_sha256`、`publication_primary_publish_command` 和 `publication_publish_commands_sha256`，Markdown report 展示同名字段。
61. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_summary`，Markdown report 展示 `publication_summary_status`、branch、latest tag、`publication_summary_target_tag`、`publication_summary_target_tag_status`、ahead count、`publication_summary_primary_next_action` 和 `publication_summary_primary_publish_command`。
62. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_summary_sha256`，Markdown report 展示同名 hash。
63. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 的 `publication_tag_publish_decision.target_tag_commands` 展示 `git tag v0.16.227` 与 `git push origin v0.16.227`，并提供 `target_tag_commands_sha256`。
64. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 的 `publication_summary` 展示 `target_tag`、`target_tag_status` 和 `target_tag_primary_command`，Markdown report 也展示同名摘要字段。
65. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 的顶层 `next_actions[0]` 等于 `publication_summary.primary_next_action`，且 cadence/readiness 动作仍保留在后续条目。
66. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `publication_blockers`、`publication_primary_blocker` 和 `publication_blockers_sha256`，Markdown report 展示同名字段与 `Publication Blockers` 小节。
67. 确认 `python scripts/release_readiness.py --target-version 0.16.227 --json` 顶层展示 `next_action_count`、`primary_next_action` 和 `next_actions_sha256`，并与顶层 `next_actions` 同源。
68. 确认 `python scripts/check_release_publication.py --json` 和 `python scripts/release_readiness.py --target-version 0.16.227 --json` 的 `next_actions` / `publication_next_actions` 不带 Markdown bullet 前缀。
69. 确认 `python scripts/check_release_publication.py --json` 顶层展示 `primary_next_action`、`next_actions_sha256`、`primary_publish_command` 和 `publish_commands_sha256`，Markdown report 也展示同名指标。
70. 确认 `python scripts/check_release_publication.py --json --publish-script /tmp/cliany-publish-release.sh` 生成的脚本注释头展示 `next_action_count`、`next_actions_sha256`、`primary_next_action`、`publish_command_count`、`publish_commands_sha256` 和 `primary_publish_command`，且 hash 与 JSON 输出同源。
71. 确认同一个 publish script 在执行前会重新运行 publication audit，并在 `next_actions_sha256` 或 `publish_commands_sha256` 漂移时退出，不继续执行过期发布命令。
72. 确认 `python scripts/check_release_cadence.py --json` 顶层 `next_actions` 不带 Markdown bullet 前缀，并展示 `primary_next_action` 与 `next_actions_sha256`。
73. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 顶层展示 `next_action_count`、`primary_next_action` 和 `next_actions_sha256`，默认文本输出与 Markdown report 也展示同名指标。
74. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --issues-dir /tmp/cliany-candidate-issues --json` 生成的 `artifact-manifest.json` 顶层也展示 `next_action_count`、`primary_next_action` 和 `next_actions_sha256`。
75. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 顶层展示 `publication_next_actions_sha256`、`publication_primary_next_action`、`publication_publish_commands_sha256` 和 `publication_primary_publish_command`，Markdown report 也展示同名指标。
76. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 顶层展示 `publication_blockers`、`publication_primary_blocker` 和 `publication_blockers_sha256`，默认文本输出、Markdown report、`artifact-manifest.json` 和 `publication-handoff.json` 也展示同名信息。
77. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 的 `publication_tag_publish_decision.target_tag_commands` 展示 `git tag v0.16.227` 与 `git push origin v0.16.227`，并提供 `target_tag_commands_sha256`。
78. 确认 `python scripts/plan_next_iteration.py --target-version 0.16.227 --json` 顶层 `next_actions` 包含目标 tag 动作和 `git tag v0.16.227`，且不再包含 `Push tag v0.16.226` 这类旧 tag 推送提示。
79. 确认 README / README.zh 路线图入口展示 `primary_next_action`、`next_actions_sha256`、`publication_blockers_sha256`、`publication_next_actions_sha256` 和 `publication_publish_commands_sha256`。
80. 确认 `docs/good-first-issues.md` 和 `docs/contributor-starter.md` 的 candidate promotion 说明同时提到 `Reproduction Context`、`Promotion Command Plan` 与 `Evidence Bundle`。
81. 维护者批准后执行 `git push origin master`。
82. 若维护者确认继续发布 `v0.16.226` tag，则回到该 tag commit 后执行 `git push origin v0.16.226`；若选择发布当前 HEAD，则先创建新的 release tag。
83. 执行 `python scripts/check_release_publication.py --remote --json` 复核远端分支和 tag。
84. 准备最终发布时，确认 `CHANGELOG.md` 的 Unreleased 移动到 `0.16.227`，并创建 tag：`git tag v0.16.227`。

## Release Notes 摘要

`v0.16.227` adds the next release draft for the local-release-publication handoff and introduces `cliany-site cases`, so maintainers can inspect real demo cases and candidate promotion next actions directly from the CLI while publication remains gated on `v0.16.226`.
