# v0.16.312

Candidate promotion evidence now keeps local adapter-generation readiness separate from live provider blockers.

## What Changed

- Added `summary.capabilities.generate_adapters.local_ready` and `local_blockers` to the doctor preflight evidence contract.
- Updated `cases --doctor-json`, the standalone extractor, case validation output, issue templates, and next-iteration artifacts to use the same 14-field template and drift hash.
- Documented how maintainers can show healthy local prerequisites alongside a blocked live provider without treating that as permission to run `explore`.

## Trust Boundaries

The real 2026-08-11 strict preflight remained retryably unavailable. This release does not claim live LLM success, a candidate package, metadata validation, online smoke, browser replay, or third-party workflow success.
