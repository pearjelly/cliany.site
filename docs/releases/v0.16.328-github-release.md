# v0.16.328

## Fixed

- Human `doctor --llm-live --require-capability generate_adapters` output now distinguishes missing LLM configuration (`E_LLM_DISABLED`) from a retryable provider outage (`E_LLM_UNAVAILABLE`).
- The JSON error envelope, strict live-LLM gate, candidate promotion boundary, and successful workflows are unchanged.

## Compatibility

- Existing doctor checks, adapter commands, SDK, HTTP API, case statuses, and release ordering remain compatible.

## Trust Boundaries

- A human-readable doctor message is local readiness guidance only. It does not prove live LLM success, candidate adapter packaging, online smoke, or third-party availability.
