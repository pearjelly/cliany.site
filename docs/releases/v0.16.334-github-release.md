# v0.16.334

## Fixed

- Candidate issue audit classifies GitHub rate-limit responses, including HTTP 429, rate-limit 403 messages, and secondary rate limits, as retryable `E_GITHUB_UNAVAILABLE`.
- Ordinary permission failures remain distinct, and the audit never synthesizes `missing` state or enters `--apply` before remote issue state is readable.

## Compatibility

- Issue creation, rewrite confirmation, candidate status semantics, CLI, adapter commands, SDK, HTTP API, and release ordering are unchanged.

## Trust Boundaries

- This release contains no candidate adapter package, live LLM success, online smoke result, or third-party workflow claim.
